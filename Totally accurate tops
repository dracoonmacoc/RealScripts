--// SERVICES
local UIS = game:GetService("UserInputService")

--// MAIN GUI (square)
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "HighlightMainGUI"
ScreenGui.Parent = game.CoreGui
ScreenGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 300, 0, 300)  -- square
MainFrame.Position = UDim2.new(0.35, 0, 0.25, 0)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame)

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -20, 0, 40)
Title.Position = UDim2.new(0, 10, 0, 10)
Title.Text = "Highlight ESP"
Title.TextScaled = true
Title.BackgroundTransparency = 1
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Parent = MainFrame

-- Buttons for highlights
local function createButton(name, yPos)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.8, 0, 0.15, 0)
    btn.Position = UDim2.new(0.1, 0, 0.2 + (yPos-1)*0.2, 0)
    btn.BackgroundColor3 = Color3.fromRGB(70,70,70)
    btn.Text = name .. ": ON"
    btn.TextScaled = true
    btn.Parent = MainFrame
    Instance.new("UICorner", btn)
    return btn
end

local categories = {Paper=true, Meteor=true}
local buttons = {}
buttons.Paper = createButton("Paper",1)
buttons.Meteor = createButton("Meteor",2)

--// HIGHLIGHT LOGIC
local highlights = {}

local function highlightPart(part, color)
    if not part or not part:IsA("BasePart") then return end
    if highlights[part] then return end
    local h = Instance.new("Highlight")
    h.FillColor = color
    h.OutlineColor = Color3.fromRGB(255,255,255)
    h.Parent = part
    highlights[part] = {highlight=h, color=color}
end

local function removeHighlight(part)
    if highlights[part] then
        highlights[part].highlight:Destroy()
        highlights[part] = nil
    end
end

local function updateHighlights()
    for _, part in pairs(workspace:GetDescendants()) do
        if part:IsA("BasePart") then
            -- Paper (case-insensitive)
            if part.Name:lower() == "paper" then
                if categories.Paper then
                    highlightPart(part, Color3.fromRGB(255,255,0))
                else
                    removeHighlight(part)
                end
            end
            -- Meteor (case-sensitive)
            if part.Name == "Meteor" then
                if categories.Meteor then
                    highlightPart(part, Color3.fromRGB(255,0,0))
                else
                    removeHighlight(part)
                end
            end
        end
    end
end

-- Auto-highlight new parts
workspace.DescendantAdded:Connect(function(part)
    if part:IsA("BasePart") then
        if part.Name:lower() == "paper" and categories.Paper then
            highlightPart(part, Color3.fromRGB(255,255,0))
        end
        if part.Name == "Meteor" and categories.Meteor then
            highlightPart(part, Color3.fromRGB(255,0,0))
        end
    end
end)

task.spawn(updateHighlights)

--// BUTTON CONNECTIONS
for cat, btn in pairs(buttons) do
    btn.MouseButton1Click:Connect(function()
        categories[cat] = not categories[cat]
        btn.Text = cat .. (categories[cat] and ": ON" or ": OFF")
        updateHighlights()
    end)
end

--// TOGGLE BUTTON GUI (small, draggable frame)
local ToggleGUI = Instance.new("ScreenGui")
ToggleGUI.Name = "HighlightToggleGUI"
ToggleGUI.Parent = game.CoreGui
ToggleGUI.ResetOnSpawn = false

local ToggleFrame = Instance.new("Frame")
ToggleFrame.Size = UDim2.new(0, 30, 0, 30)  -- smaller, easier to drag
ToggleFrame.Position = UDim2.new(0.01, 0, 0.9, 0)
ToggleFrame.Active = true
ToggleFrame.Draggable = true
ToggleFrame.BackgroundColor3 = Color3.fromRGB(35,35,35)
ToggleFrame.Parent = ToggleGUI
Instance.new("UICorner", ToggleFrame)

-- Detect clicks on frame itself
ToggleFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or
       input.UserInputType == Enum.UserInputType.Touch then
        MainFrame.Visible = not MainFrame.Visible
    end
end)
